# Assets

This folder is for local, non-code assets that support the project.

- `videos/` holds the provided demo footage.

The `.mov` files are intentionally ignored by git so the repo stays compatible with normal GitHub limits. If you later want to version them remotely, switch that folder to Git LFS.
